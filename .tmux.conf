# remap Ctrl-b to Crtl-a (more ergonomic)
unbind C-b
set -g prefix C-a
bind -n C-a send-prefix

setw -g mode-keys vi

set-option -g allow-rename off
set-option -g pane-border-status top
set-option -g pane-border-format " [ ###P #T ] "
