$${HOME}/.config/git/ignore: .config/git/ignore
	cp $^ $@

$${HOME}/.gitconfig: .gitconfig
	cp $^ $@

$${HOME}/.zshrc: .zshrc
	cp $^ $@

$${HOME}/.vimrc: .vimrc
	cp $^ $@

$${HOME}/.gvimrc: .gvimrc
	cp $^ $@
